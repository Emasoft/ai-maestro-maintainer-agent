---
trdd-id: K9M2X7Q4
title: Align plugin with Claude Code 2.1.181 to 2.1.200 changelog
column: dev
created: 2026-07-03T21:52:00+0200
updated: 2026-07-03T21:52:00+0200
current-owner: ai-maestro-maintainer-agent
task-type: docs
release-via: publish
test-requirements: [lint]
relevant-rules: []
implementation-commits: []
---

# TRDD-K9M2X7Q4 — Align plugin with Claude Code 2.1.181 to 2.1.200

## STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-07-03T21:52:00+0200

**Current state:**
- README model prose: FIXED (working tree; commit pending) — was "runs on Opus
  4.8"; now "model: inherit picks up the session default = Sonnet 5 (CC >= 2.1.197,
  1M ctx), or Opus 4.8 when selected". (README.md ~L159-162)
- **NEXT ACTION:** pin the CPV validator ref in `.github/workflows/release.yml:49`.
  `uvx --from git+https://github.com/Emasoft/claude-plugins-validation` has NO
  `@ref`, so the release security gate runs whatever CPV default-branch HEAD is at
  release time (supply-chain drift — the exact class this plugin guards against).
  Gate STRUCTURE is fail-closed (`needs: validate-tag`, no `if: always()`).
  DECISION NEEDED before editing: pin-to-tag (reproducible, must bump each CPV
  release) vs `@main` (explicit-latest, still drifts) vs full SHA — for OUR OWN
  gating tool. Not a rushed edit.
- Then: publish v1.7.7 bundling both (publish.py --patch).

**Changelog audit (verified against the repo, NOT assumed):** only 2 surfaces
bind; every other 2.1.181–2.1.200 item is CLI-internal runtime behavior needing no
plugin change.
- Sonnet 5 = new default (2.1.197) -> README prose (done).
- release.yml unpinned CPV validator -> audit finding (NEXT ACTION above).
- N/A, confirmed by grep: model IDs (no hardcoded `claude-*` IDs in plugin);
  `/agents` wizard removed 2.1.198 (not referenced — hits were `$HOME/agents/`
  paths); permission default->manual 2.1.200 (no perm-mode refs); hook matchers
  comma/hyphen exact-match 2.1.191/195 (single `*` SessionStart matcher, safe);
  `.mcp.json` self-approval 2.1.196 (no `.mcp.json` shipped); destructive-git
  auto-block 2.1.183 (commit --amend targets the agent's OWN unpushed commit =
  allowed; `git checkout -- "$file"` in workflow-pin-actions reverts the skill's
  own failed pin = legitimate self-rollback).
- `model: inherit` on the main agent confirmed CORRECT (CA-04) — it auto-picks the
  new Sonnet 5 default with zero code change.

**Durable evidence:** `reports/maintainer-release-audit/20260703_214848+0200-release-yml-audit.md`

## Scope

Bring docs + release workflow inline with CC 2.1.181–2.1.200. No agent/skill code
change is required for the model shift — `model: inherit` handles it. Two concrete
edits: README prose (done) + release.yml CPV-ref pin (pending decision).
