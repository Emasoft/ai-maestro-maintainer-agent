---
trdd-id: K9M2X7Q4
title: Align plugin with Claude Code 2.1.181 to 2.1.200 changelog
column: published
created: 2026-07-03T21:52:00+0200
updated: 2026-07-03T22:03:51+0200
current-owner: ai-maestro-maintainer-agent
task-type: docs
release-via: publish
test-requirements: [lint]
relevant-rules: []
implementation-commits: [ddc1202, d1f1691]
published-version: 1.7.7
published-at: 2026-07-03T22:03:51+0200
---

# TRDD-K9M2X7Q4 — Align plugin with Claude Code 2.1.181 to 2.1.200

## STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-07-03T22:03:51+0200

**Current state: DONE — shipped in v1.7.7. No NEXT ACTION.**
- README model prose: FIXED + committed (ddc1202) — "model: inherit picks up the
  session default = Sonnet 5 (CC >= 2.1.197, 1M ctx), or Opus 4.8 when selected".
- release.yml CPV validator ref: PINNED + committed (d1f1691). Decision taken =
  pin-to-tag (reproducibility IS the security property). `release.yml:50` now
  `git+https://github.com/Emasoft/claude-plugins-validation@v2.152.1` (latest CPV
  release). VERIFIED at that tag: `cpv-remote-validate` entry point + positional
  `plugin` subcommand + `--strict` pass-through to validate_plugin all present, so
  the gate invocation is unbroken. `# WHY:` comment added above the line.
- PUBLISHED: v1.7.7 shipped via `publish.py --patch`. All gates green (lint+typecheck,
  533 tests, CPV remote --strict = 0 blocking issues; the RC-PIPELINE-DRIFT-001
  warnings are advisory/ahead-of-canon, non-blocking — do NOT --force-templates).
  Local tag v1.7.7 + GitHub release (draft=false) live; 0 unpushed; tree clean.
- NOTE (possible follow-up, NOT this task): CPV-ref version skew across 3 callsites —
  release.yml post-hoc gate is now @v2.152.1, while publish.py's local gate + ci.yml
  validate stay @v2.143.0. Aligning all three is a future TRDD; left as-is here
  (out of scope; RULE 1 stay-focused).

**Changelog audit (verified against the repo, NOT assumed):** only 2 surfaces
bind; every other 2.1.181–2.1.200 item is CLI-internal runtime behavior needing no
plugin change.
- Sonnet 5 = new default (2.1.197) -> README prose (done).
- release.yml unpinned CPV validator -> audit finding (NEXT ACTION above).
- N/A, confirmed by grep: model IDs (no hardcoded `claude-*` IDs in plugin);
  `/agents` wizard removed 2.1.198 (not referenced — hits were `$HOME/agents/`
  paths); permission default->manual 2.1.200 (no perm-mode refs); hook matchers
  comma/hyphen exact-match 2.1.191/195 (single `*` SessionStart matcher, safe);
  `.mcp.json` self-approval 2.1.196 (no `.mcp.json` shipped); destructive-git
  auto-block 2.1.183 (commit --amend targets the agent's OWN unpushed commit =
  allowed; `git checkout -- "$file"` in workflow-pin-actions reverts the skill's
  own failed pin = legitimate self-rollback).
- `model: inherit` on the main agent confirmed CORRECT (CA-04) — it auto-picks the
  new Sonnet 5 default with zero code change.

**Durable evidence:** `reports/maintainer-release-audit/20260703_214848+0200-release-yml-audit.md`

## Scope

Bring docs + release workflow inline with CC 2.1.181–2.1.200. No agent/skill code
change is required for the model shift — `model: inherit` handles it. Two concrete
edits: README prose (done) + release.yml CPV-ref pin (pending decision).
